# User-Configuration-Manager-Project
This User Configuration Manager is a Python-based utility I built, designed to handle dynamic user preferences (such as UI themes, languages, and notification toggles).  The system utilizes a mutable Python dictionary as its core data store (test_settings) and exposes a clean API through four primary CRUD (Create, Read, Update, Delete) functions.
